{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--device", "iPhone 15",
        "--user-data-dir", "./.pw-profile"
      ]
    }
  }
}
