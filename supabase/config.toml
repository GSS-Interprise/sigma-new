project_id = "zupsbgtoeoixfokzkjro"

[functions.evolution-api-proxy]
verify_jwt = false

# Auto-reconnect de chips presos em "connecting" (mitigação do flapping Baileys/proxy).
# verify_jwt=false: invocada pelo pg_cron a cada 5 min.
[functions.chip-auto-reconnect]
verify_jwt = false

[functions.disparos-zap-pendentes]
verify_jwt = false

# Healthcheck E2E (v2). verify_jwt=false pra ser invocado pelo pg_cron sem header trocado.
[functions.bridge-healthcheck-v2]
verify_jwt = false

# Edges chamadas com alta frequência (campanha-ia-responder dispara a cada msg incoming).
# verify_jwt=false reduz overhead de auth check em chamada interna do bridge N8N.
[functions.campanha-ia-responder]
verify_jwt = false

[functions.campanha-disparo-processor]
verify_jwt = false

[functions.campanha-cadencia-processor]
verify_jwt = false

[functions.campanha-qa-handoff-handler]
verify_jwt = false

[functions.campanha-qa-relay]
verify_jwt = false

[functions.campanha-opt-out-handler]
verify_jwt = false

[functions.campanha-email-sender]
verify_jwt = false

[functions.lead-perfil-extrator]
verify_jwt = false

[functions.bridge-healthcheck]
verify_jwt = false

[functions.chip-healthcheck]
verify_jwt = false

# Recebe webhook CONNECTION_UPDATE do bridge N8N — classifica disconnect e
# atualiza chip_state/chip_health_event. Plano Anti-Ban v1 Sprint 1.
[functions.chip-disconnect-classifier]
verify_jwt = false

# Sprint 2 antiban — persona-generator gera persona BR aleatória pra novo chip.
# verify_jwt=false pra ser invocada internamente pelo chip-bootstrap.
[functions.persona-generator]
verify_jwt = false

# Sprint 2 antiban — chip-bootstrap orquestra criação completa:
# Evolution instance + proxy Bright Data + webhook + chip_state + persona.
# Chamada pela UI quando admin cria chip novo.
[functions.chip-bootstrap]
verify_jwt = false

# Operação administrativa de rollout; exige JWT válido e usa service_role internamente.
[functions.chip-webshare-rollout]
verify_jwt = true

[functions.sigzap-history-sync-worker]
verify_jwt = false

# Sprint 2 antiban camada 2 — aquecedor-tick é chamado por pg_cron a cada 2min.
# Pra cada chip em aquecimento/pronto/producao, decide se gera evento orgânico
# (sorteia parceiro power-law, gera mensagem via OpenAI, envia via helper).
[functions.aquecedor-tick]
verify_jwt = false

# OAuth callback do Google — chamado pelo browser do usuário sem header Authorization.
[functions.google-oauth-callback]
verify_jwt = false

[functions.admin-bulk-reset-pw]
verify_jwt = false

# Recebe POST do n8n (captadores Effecti GSS e AGES) com token proprio
# `sigma_...` validado DENTRO da edge — nao e' JWT. verify_jwt=true aqui
# derruba a captacao inteira com "Invalid JWT" (aconteceu 22/07: deploy sem
# --no-verify-jwt reativou a verificacao). NUNCA remover esta entrada.
[functions.api-licitacoes]
verify_jwt = false
